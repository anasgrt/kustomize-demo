# k8s/cache/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      app: cache
      tier: backend

resources:
  - cache-deployment.yaml
  - cache-service.yaml
